package main

import (
	"github.com/joshuablais/GoSubscribe/internal/config"
	"net/http"
)

func Routes(mux *http.ServeMux, cfg *config.Config) {
	// TODO - Set up handler for subscribe
	// mux.HandleFunc("POST /subscribe", (pages.ButtonsPage(cfg)))
}
